# Chekin — UX Prototypes

Static HTML/CSS mockups (no build, no app logic, mock data) of redesigned Chekin surfaces, layered on top of the existing `@chekinapp/tokens` design system.

Four prototypes live here, behind a common chooser at the root `index.html` ([GitHub Pages](https://vlad-svr.github.io/guestapp-redesign-prototypes/)):

- **`dashboard/`** — the host-side property management app: action-queue Home, bookings pipeline with clickable KPI filters, table-based properties, property workspace and workspace settings with separated views, unified documents hub, billing, 7 switchable design variants. See `dashboard/UX-AUDIT.md` for the audit and "what changed and why".
- **`guestapp/`** — the guest-side Redesign 2.0 flows documented below.
- **`guestapp/sdk/`** — the **embeddable Guest SDK (ChekinPro)** in a neutral skin, shown inside a mock partner website.
- **`guestapp/sdk-guestapp/`** — the same SDK flows in the guestapp's design language. See below.

## Guest SDK prototypes

The SDK is the same product as the guestapp, but rendered **inside a partner's page**, so it gets its own design study — two candidate surfaces, drawn from **markup-identical** pages that differ only in `shared.css`:

- **`guestapp/sdk/` — neutral.** The language of the hosts-sdk in `dashboard-chekin`: muted slate `#505077`, the host's **system font stack** (an SDK must not force a webfont download on the partner), no photography, no gradients or glass, depth from 1px borders.
- **`guestapp/sdk-guestapp/` — guestapp style.** Poppins, Chekin blue `#385bf8`, brand gradients, a glass widget header (chrome-only), rounder radii and real elevation.

A **Design: Neutral · Guestapp** toggle on every flow page jumps to the *same screen* in the other skin — the two folders share filenames and screen ids — so the pair doubles as a live demo of what a `guest-sdk.css` surface file in `@chekinapp/tokens` (beside `guestapp.css`) would control.

**Scope** (confirmed with the team): home hub · guests summary · guest form (police field sets, `hiddenSections`, `prefillData`, signature) · autofill by OCR · identity verification (+ QR handoff to phone, + QR-IV on property) · property link · guidebooks & FAQ · remote access & keys · errors & recovery. **Deliberately excluded**, because the Guest SDK does not carry them: payments, tourist taxes, deposits/property protection, upselling, chat, check-out, eSIM, instant check-in/auth, kiosk.

Conventions specific to these two folders:

- The mock partner site ("Nordica Stays") carries its **own green brand**. The **Widget accent: Neutral · Host brand** toggle repoints one CSS variable so the widget adopts that green — standing in for the SDK's real `styles` / `stylesLink` injection.
- An **integration event console** under each frame logs the ChekinPro callbacks (`onGuestRegistered`, `onIVFinished`, `onError`, `onScreenChanged`…) as you click through, and every page ends with a note naming the config options behind it (`mode`, `hiddenSections`, `redirectIVQrUrl`…).
- **Everything stays inside the widget box** — sheets, modals, spinners and crash states — mirroring the real SDK's iframe. The partner page never breaks.
- The **Embed: Desktop · Mobile** toggle narrows the same DOM to a phone-width host page; there are no separate mobile files.
- The in-widget **language chip** appears on every stable screen (guests read legal text), while the **menu button** appears only on `mode: "ALL"` pages — scoped modes like `ONLY_GUEST_FORM` and `IV_ONLY` stay single-purpose by contract.
- `flow.js` and `modals.js` are copied **verbatim** from `guestapp/`; `chrome.js` (toggles, sheets, event console) is shared byte-for-byte by both variants.

## How to view

Open the root `index.html` in a browser and pick a surface — or open `guestapp/index.html` directly; it links to all guest prototypes and documents every proposed change with its rationale.

**Proposal vs current.** Where a feature has two prototypes, the one marked **Proposal** is a redesign
proposal shown *beside* the **current** shipped variant — it is not the main variant, and nothing is
decided until it is chosen. The floating nav and `index.html` badge them the same way.

One canonical prototype per feature and form factor — organized by feature, with 📱 mobile and 🖥 desktop grouped together. Most are click-through flows; two are static screen sets (Home, desktop registration). `index.html` and the floating nav on every page follow the same feature-first grouping.

| File | Feature / form factor | Contents |
| --- | --- | --- |
| `SPEC.md` | — | **Flow spec for AI agents** — states, transitions, CTA decision tables. Must be updated in the same commit as any UI change here (see its sync contract) |
| `index.html` | — | Overview + "what changed and why" for each improvement |
| `flow-splash-v2.html` | Splash screen · 📱 | A rebuild of `components/SplashScreen`, shown **beside a faithful V1 replica — both clocked**. The shipped component holds every guest for **`load + 5000 ms`** (the timer starts *after* the data arrives), closes two curtains over its own logo at **4.3 s** — leaving an opaque brand-coloured rectangle with no logo, loader or text until dismissal — and, when auth-init hangs, **never unmounts**, because that 5 s timer is its only timer and it is gated behind the data having already landed. Here the splash is a **handoff, not a gate**: it is `position:absolute` over a **shape-matched skeleton** of the screen it becomes, held for **`max(load, 500 ms)`**, and on ready the surface fades while the mark **travels to the header logo slot**. A four-rung timing contract replaces the single constant — **silent below 1.2 s**, "taking longer than usual" + elapsed clock + *Try again* at **6 s**, a **watchdog** at **20 s** that renders a real error state (*Try again* · *Find my booking* · *Message your host* · copyable reference). The progress ring **eases to 90 % and stops**, completing only when the data lands, and drops to indeterminate past 6 s rather than freezing on a lie. Adds the **offline**, **host-branded** and **splash-disabled** states the component has never had; the host logo cross-fades inside a **reserved 96 px box** instead of reflowing, and `--sp-ink` is derived from the host background's luminance so navy-on-navy is impossible. Seven states + the annotated V1 defects. Carries a **Style: Classic · Modern** switch (`splash-toggle.js`): **Classic** is the refined V1 — flat brand background, bare logo, the three pulsing dots, curtains removed; **Modern** is the pre-auth idiom (branded gradient, colour orbs, film grain, liquid-glass plate, honest ring). Both share every behavioural and accessibility fix |
| `flow-splash-desktop-v2.html` | Splash screen · 🖥 · SDK · kiosk | The same handoff at three sizes: the laptop shell (the mark lands on the **navy sidebar's brand tile**), the **SDK widget embedded in a mock client website** (the splash is bounded by the widget box, never the host page), and the **kiosk** at one-metre reading distance — where the watchdog matters most, because today a hung request leaves an unattended lobby screen showing a solid blue rectangle until someone reboots it |
| `home.html` | Home · 📱 (current) | Home / check-in list (static screens): glass chrome, arrival countdown, "next up" spotlight, all-done state, "Handpicked for your trip" recommendations (native offers + Mozio "HOT DEAL" + Airalo eSIM banners) and a GuruWalk Free-offers FAB |
| `home-desktop.html` | Home · 🖥 (current) | Home (static screens): navy sidebar with progress module, hero, checklist with ring + timeline, right rail, "Handpicked for your trip" 3-col recommendations with the Mozio wide banner + Airalo eSIM banner, Free-offers FAB |
| `flow-auth-mobile.html` | Sign in & onboarding · 📱 | Interactive instant check-in entry (`pages/Auth`, gated by `isInstantCheckInEnabled`): the **create-account machine** (email → password with show/hide + strength → 6-cell OTP with a resend countdown), returning **log in**, Google OAuth (mock popup) and an honest "continue without an account"; the **Welcome** moment; then the four **Onboarding** steps as choice cards on a labelled step rail — reservation type (family/group/tourist), guest counters, who's-coming (avatars, registered locked-checked) + agreement clause, lead guest (radio + Crown); ending in the **guest-creation success** modal that hands off to registration. Pre-auth uses the branded gradient + liquid-glass card |
| `flow-auth-desktop.html` | Sign in & onboarding · 🖥 | The same entry as a **brand-panel split** (persistent gradient panel selling the account + property, beside a focused form column): create-account, OTP with countdown, returning-login, welcome, and the four onboarding steps as choice cards on a labelled rail (dark-on-light) |
| `flow-search-v2.html` | Find booking · 📱 | **Proposal.** The arrival card (`pages/SearchReservation`): the property becomes the hero — a CSS-painted **photo canvas** with the "Official check-in" seal, the name, the city and the **host strip** (avatar · 4.9 · "expecting you on 14 July") — and the search floats over it on a frosted sheet. Method cards advance on one tap; the booking code goes into **eight segmented cells** (auto-typed, with a Paste chip, CTA disabled until the last one lands) plus a "where's my code?" sheet; the wait is a mini property card under a **scan line** above a ticking three-step checklist; "found" pays off as a **boarding pass** (dates · nights · copyable reference · lead guest · QR, with the tear notches masked out of the card). Not-found stays actionable (retry · other method · new stay · contact the named host); the create-reservation form keeps its groups + "From your search" tags |
| `flow-search-desktop-v2.html` | Find booking · 🖥 | **Proposal.** The desktop arrival card: a **full-height property canvas** on the left (photo, host, a past-guest quote, trust badges) that never leaves — the guest sees where they're going while they search, while they wait and when it's found — beside a frosted card carrying the same nine screens |
| `flow-search-mobile.html` | Find booking · 📱 (current) | Interactive pre-check-in search (`pages/SearchReservation`): property-context chip → method picker as descriptive **tap-to-continue cards** (booking reference / date + email) → booking-code entry with a "where's my code?" sheet → searching **radar** → booking-found → registration; plus the date+email branch, the actionable **not-found** state (retry / other method / contact host), the grouped **create-reservation** (property-link) form with "From your search" tags, and the direct-link auto-search |
| `flow-search-desktop.html` | Find booking · 🖥 (current) | The same search as a centered **pre-auth card** on the branded gradient (no app shell yet — search runs before a reservation exists): method cards, booking-code with the help dialog, date + email, grouped create-reservation, searching radar → found, actionable not-found, direct-link auto-search |
| `flow-iv-mobile.html` | Identity verification · 📱 | Interactive IV flow: passport/ID branches, liveness, upload → crop, QR entry, email + SMS contacts, all detours |
| `flow-iv-desktop.html` | Identity verification · 🖥 | Interactive desktop IV flow: webcam vs phone choice, QR live sync, document choice, back side, contacts, detours |
| `flow-housing-iv-v2.html` | IV QR · 📱 | Identity verification at an on-property housing. The guest scans the **entrance QR**: the SDK page (`pages/VerifyOnHousing` → `pages/FindReservationsByName`, mode `QR_IV_ON_PROPERTY`) finds the booking by name and hands off to the regular guestapp, where IV opens straight away — *the scan at the door was the proof*. When that proof is missing, `IVQRGuard` holds IV shut and routes the guest through `IVQRScannerView` to scan the sticker **in-app**. **No invented stepper** on the pre-auth screens — the IV step count is unknowable there (passport vs ID card, selfie vs liveness vs neither), so progress is carried by copy. Inside IV the door scan is **step 1 of IV's own stepper**, so progress advances across the handoff instead of restarting; on desktop the scanner renders in the full IV shell with the **Vela rail**. Fixes: the `IncompleteDataModal` **inlined onto the field it asks about** (naming the field the server actually requested), the confirm-the-match beat built from the discarded `guest_name`, an actionable not-found, the invalid-QR link state, and the corrected `InvalidQRModal` copy. Renders `IVUnavailableView` **verbatim from the IV flow** rather than inventing a second one. Flags the **`?scanned=true` presence flag that never gets read**
| `flow-housing-iv-desktop-v2.html` | IV QR · 🖥 | The same flow on the door-mounted tablet. The entrance-QR arrival keeps the full-height property canvas; the in-app scanner and the shared IV steps render in the **real guestapp shell** (navy sidebar, "Back to summary"), with a URL bar that moves from `/verify-on-housing` to `/:token/verification/qr-scan` as the guest crosses into their own session
| `flow-guests-v2.html` | Guests summary · 📱 | Interactive roster (`GuestRegistrationView` + `GuestsPanel` + `GuestBox`), shown **side by side with today's hub** (the `#r-hub` screen lifted verbatim from `flow-registration.html`, static and inert — the scenario chips drive the V2 phone only). Deliberately close to the shipped layout: same header, same sections, same one-row-per-guest list. What changes: **one progress line + a segment per guest** (replacing a pill and a bar that read different fields); **action-named statuses** (Registered / Verify ID / Add details / Not started — each an icon + a word + a rail shape, never colour alone); rows as real `<a>`/`<button>` with the kebab as a **sibling**, not a nested control; a **named slot per missing guest**; a single docked CTA naming *which guest and which task*, with the matching row washed blue; the **delete-confirm dialog + 10-second Undo toast**; the **Edit group** sheet. Six states: partial · nobody registered · solo · everyone registered · shape-matched skeleton · background refresh (a 2 px sweep, not a 50 %-opacity list) |
| `flow-guests-desktop-v2.html` | Guests summary · 🖥 | The same roster as the navy-sidebar **"Guests"** destination: progress line, meter and list in a reading column, and the mobile dock reborn as a small **action ledger** (next guest · next task · the reward it unlocks). Partial, nobody-registered, everyone-registered and skeleton states |
| `flow-registration.html` | Guest registration · 📱 | Interactive registration flow: hub → add details (scan-to-autofill) → review & **sign** (tap the pad → draw in the signature sheet → preview) with agreements + contract opt-in; plus the required-signature error |
| `guest-registration-desktop.html` | Guest registration · 🖥 | Desktop hub + two-column review form with auto tags, **inline draw-on-pad signature** (draw directly with mouse/trackpad), and agreement clauses (static screens) |
| `flow-autofill-mobile.html` | Autofill · 📱 | Interactive MRZ scan modal (`AutoFillFlowModal`): entry card → scan (document-in-frame with the highlighted **MRZ band** + scan line + "Fit the code" pill) → reading (OCR + decoded MRZ strip) → **Review your details** (scan summary + ✦ Scanned tags + a Check flag + IV note); plus the barcode document-selection step, couldn't-read retry, camera-blocked recovery, and the Need-help MRZ sheet |
| `flow-autofill-desktop.html` | Autofill · 🖥 | The same modal as a centred **530 px dialog** over a dimmed form; the review payoff lands on the desktop registration shell — same scan/reading/select/fail states |
| `flow-registered-modal.html` | Registration complete · 📱🖥 | Interactive "guest registered" modal: offers load during the wait, status flips mid-browse, cart, no-offers, error |
| `flow-payments-mobile.html` | Payments · 📱 | Interactive payments flow: cart with charges-vs-holds ledger, pay later, wallet/card form, 3-D Secure bank step, live confirmation timeline (+ slow fallback), receipt, decline recovery, order history, empty cart, deposit chooser |
| `flow-payments-desktop.html` | Payments · 🖥 | Interactive desktop payments: two-column cart with sticky ledger, card form, confirmation timeline, receipt celebration, decline recovery, order history, deposit chooser — stage-aware Vela rail |
| `flow-deposit-mobile.html` | Property protection · 📱 | The standalone `DepositView` (`/deposit`), reached from Home — not the deposit chooser embedded in the payments cart (`p-deposit`), though the two now share one card. Every option leads with **what leaves the account today** (€0.00 held vs €39.00 charged) and the hold's unnamed cost — **€300.00 of card limit frozen for the stay** — becomes an amber bullet. The three backend shapes each get an honest screen: **one protection** (no question asked, because there is no choice), **two protections** (radios, because they *are* exclusive — the real UI ships checkboxes bound to one field), **optional** (pay online / pay on arrival, plus a decline that now confirms). Plus the auto-opening `EnsureSecurityPropertyModal`, edit-from-cart with live totals, the paid state that today is a **silent redirect home**, the pre-auth-vs-retention info sheet, the damage-waiver reader and the skeleton |
| `flow-deposit-desktop.html` | Property protection · 🖥 | The same decision as a **"Property protection"** sidebar destination between guest registration and payments. The two options stand side by side so the trade is one glance rather than one scroll, with a stage-aware Vela rail answering the only question anyone has ("is the €300 taken from my account?") |
| `flow-book-experience-mobile.html` | Book an experience · 📱 | The booking chain behind an offer, which the upselling flow only ever pointed at: `BookExperienceView` (quantity / slot / delivery form) → `EnterContactEmailView` → `CreateDealCompleteModal`, plus the `/:token/:slug` deep link (`OfferSlugRedirectView`). Closes three spec gaps — the **delivery date & time picker** (day strip bounded by check-out, hour slots from the host's `duration_availabilities`), **multi-item offers** (`MULTIPLE_PRICE`: named baskets, each with its own price and counter) and the **slug deep-link** (a loader that names what it's opening; a dead slug that names what's gone). Also fixes the completion modal, which today tells an **on-request** guest their booking is confirmed and offers them a cart that cannot hold it. **Mobile only** — on a laptop both views render `OfferView`, so the desktop story stays `flow-upselling-desktop.html` |
| `flow-taxes-mobile.html` | Tourist taxes · 📱 | Interactive taxes flow: per-guest math card (taxed nights × rate, dashed €0.00 exempt rows), exemption picker with live new-total preview, new-guest exemption, edit-from-cart mode, age-priced region, paid/locked state |
| `flow-taxes-desktop.html` | Tourist taxes · 🖥 | Interactive desktop taxes: math card + sticky total ledger with the confirm CTA, exemption picker with delta preview, edit-from-cart mode, paid/locked state — stage-aware Vela rail |
| `flow-upselling-mobile.html` | Upselling · 📱 | Interactive extras & experiences flow: category pill rail, offer grid with ⚡ Instant / 🕗 On request lane chips, exclusive featured card, instant detail with quantity + "nothing charged now", on-request detail with inline reply email, "My requests" lifecycle tracker (dashed waiting amounts), declined-with-plan-B, partner offers (dashed, external), no-extras empty state |
| `flow-upselling-desktop.html` | Upselling · 🖥 | Interactive desktop upselling: sidebar "Extras & experiences" destination, 3-column grid with lane chips, detail pages with sticky booking box, request tracker in the booking box, "My requests" with sticky ledger (waiting = dashed row), declined state — stage-aware Vela rail |
| `flow-faq-mobile.html` | FAQ & language · 📱 | Interactive FAQ + language: task-grouped accordion with action links, highlight search, no-results with escape actions, language bottom sheet (18 languages, "Suggested" group), instant switch to Español in place |
| `flow-faq-desktop.html` | FAQ & language · 🖥 | Desktop FAQ as a sidebar "Support" destination, highlight search, language dialog (two-column grid), full-shell Español switch — stage-aware Vela rail |
| `flow-remote-access-mobile.html` | Virtual keys · 📱 | Interactive remote-access flow: keys-ready with tactile ⇢ slide-to-open + keypad code (copyable), the "not available yet" gate as a conditions checklist that deep-links to the blocking task, before-the-window locked state, Keyless partner-app + booking reference, building with many Salto doors (one offline), opening → opened → connection-issue status modals |
| `flow-remote-access-desktop.html` | Virtual keys · 🖥 | Interactive desktop remote-access: navy sidebar "Virtual keys" destination, keys in a reading column with the slide-to-open, gate checklist, before-the-window, multi-door building — stage-aware Vela rail (door-status legend, "the last step" deep-link) |
| `flow-guidebooks-mobile.html` | Guidebooks · 📱 | Interactive guidebooks (`GuidebooksListView` + `GuidebookView`): browsable **cards** with a cover band, a **type badge** (Guide / Web / PDF from `content_type`) and **preview chips** of what's inside; cross-guidebook **search** with highlighted matches; the rich EDITOR guide with a sticky **"In this guide" chapter rail**, a **Wi-Fi card with copy**, check-in rows, a CSS-drawn **map block + Directions sheet**, amenities grid, video and file blocks; **recommendations** as place cards (distance · rating · directions) on a pinned map; the **PDF reader** (doc toolbar: page count · download · full screen) and **HTML reader** (web-page/auto-translated badge); the **not-available gate** (reasons → deep-linked task) and the **empty** state (→ Ask Vela / FAQ); plus the jump-to-guidebook drawer and the Vela sheet |
| `flow-guidebooks-desktop.html` | Guidebooks · 🖥 | The same as the "Guidebooks" sidebar destination: a **card grid**, and a reading view where the long guide's chapters live in the **Vela rail** as a live "In this guide" index (+ jump-to-other-guidebooks and Ask-Vela); recommendations grid, the PDF reader and the not-available state |
| `flow-housing-guidebooks-mobile.html` | Housing guidebooks · 📱 | The **property-link** surface (`/housing-guidebooks` → `HousingGuidebooksLayout` + `HousingGuidebooksView` + its own `GuidebookView`): guidebooks for a **home**, opened without a booking. Same `GuidebooksList` cards as `flow-guidebooks-*`, a different shell — the host's brand (`usePropertyLinkInfo`: logo · `display_name`) replaces the booking reference, and the reservation chrome (progress ring, bottom nav, Vela) is **absent because there is no reservation**. The hamburger opens the real right-side nav drawer listing every guidebook + Language / FAQs / Privacy. Fixes three shipped faults: **PDF and Web guidebooks escape the layout** (`useGuidebookNavigate` routes them to `/guidebook-{html,pdf}/:id`, outside the housing shell), the mobile header offers **no way back**, and the per-language group fallback (`locale === i18n.language ?? is_default`) is **silent**. Plus the list skeleton and a `no_guidebooks_available` empty state with a real exit |
| `flow-housing-guidebooks-desktop.html` | Housing guidebooks · 🖥 | The same property link on a laptop: the persistent **284px nav** (`grid lg:grid-cols-[284px_auto]`) lists every guidebook the home has, so the whole property is one click deep from anywhere; the sidebar belongs to the host, not the booking. The 94px `contentTitle` header, the "Back" link, and the PDF/Web readers **inside** the shell |
| `flow-keycards-kiosko.html` | Key cards · kiosk | The `/remote-keys` route (`components/RemoteAccess/RemoteKeys`), which despite its name is **not** virtual keys — it drives the **physical card dispenser** under the kiosk screen (`printCard({room_number, guest_name})` over the kiosk websocket). `ProgressTaskBar`'s abstract rail becomes a **stack of real cards** (printed = solid with a chip, remaining = dashed ghosts, printing = the one in the slot), and every screen that expects the guest to reach for something **points at the dispenser** — the bezel lights up with it. Covers the **auto-opening `RegisteredGuestsInfoModal`** (fires when `known_number_of_guests > added_number_of_guests`, undismissable for the first guest), the print → dispense loop (`StatusModal` LOADING → SUCCESS → `PrintedCardsModal` after 3 s), both `PrintedCards` branches (more owed / all printed), the **dispenser jam** (routes to the front desk, never a retry) and the **no-room-assigned** gate (cause stated *above* the disabled button). Kiosk-sized: 84px buttons, 34px titles, one-metre reading distance |
| `flow-travel-guide-v2.html` | AI Travel Guide · 📱 | A rebuild of `pages/TravelGuideView` (`TravelGuideStartPage` + `TravelGuideQuestions` + `TravelGuideLoading` + `TravelGuidePlan`). The generated itinerary stops being a **one-shot artifact**: the shipped app force-forwards `intro → guide` the moment a guide exists (`TravelGuideView.tsx:70`) and ships **no regenerate control anywhere**, so a guest who tapped "Skip questions" is stuck with that plan for the whole stay. Here the plan header renders `profile_assumptions` as a **"Tuned for" strip** beside a **Refine** button, and the rebuild confirm names **what survives** (cart additions, anything already paid). Six questions on one scroll become **three required + three folded optional**, on real `radio`/`checkbox` groups — a re-tap can no longer empty a required answer — with **soft caps** ("pick up to 3 — a narrower answer makes a sharper plan") and a meter whose segments map to *questions*, not to an answered count. The submit is **never disabled**: pressing it unsatisfied flags every gap, focuses the first and announces it. The build screen gains an **elapsed clock beside the real estimate** (40–60 s, not "a few seconds" over a 5.5 s × 4-phase poll), a **Cancel**, a "you can leave this page" note, and a **Still working** state past 90 s; failure gains **three exits** and a copyable run reference. The plan gains a **"Right now" card** (time · next stop · walk time · Directions), real `role="tablist"` phase tabs with roving arrow keys, `<h3>` time-of-day headings, a **start-time chip** per slot, **Directions + Opening hours on every `web_search` slot** (which today render *no action at all*), a cost band that is dots + word + an `sr-only` sentence, and a **"Why this pick"** panel carrying the answer that drove the pick and the `source_url` behind it. Plus `TripGlance` on the phone (today `hidden lg:block`), an **offline** saved plan, a shape-matched skeleton, and an empty state that names the answers that emptied it. Sheets: Refine, Directions, Take it with you |
| `flow-travel-guide-desktop-v2.html` | AI Travel Guide · 🖥 | The same four views as the navy-sidebar **"Travel Guide"** destination. A laptop cannot walk to Mercat Central, so the mobile "Right now" card becomes a persistent **trip rail**: at-a-glance totals (with the **planned spend** the shipped `TripGlance` omits), the profile the plan was tuned to with the way back into the questions, a **"Where this came from"** card that finally renders `source_urls`, and a **Send to my phone** hand-off. Intro, questions, build, failure, plan, skeleton and empty |
| `flow-checkout-mobile.html` | Check-out · 📱 | Interactive check-out (`pages/Checkout`: `BeforeYouLeaveView` → `RateYourStayView` → `ThanksView`): the empty payment box becomes a guided **settle → rate → done** flow with a segmented spine. A **"you're all set" checklist** (guests registered · stay paid · keys-in-lockbox) reassures first; the **deposit hold** stays hollow and out of the payable total (SPEC hold convention); the never-built **"add before you go"** services (late check-out `AUTO`, luggage drop) get their surface, wired to a live ledger + 6% fee, with an **honest €0.00 "continue"** path when nothing is added. The star rating gains an **emotive verdict** and keyboard support; **≥4 → thanks** (optional Google / Booking.com reviews + Mozio airport transfer), **≤3 → service recovery** (message host / call back — never public reviews). Plus the add-on **receipt** and the **already-checked-out** summary. Modals: keys how-to, fee explainer, transfer times, host message, Vela sheet. Carries a **Standard ↔ Liquid glass** style toggle (`checkout-toggle.js`) that frosts the reading surfaces over color orbs |
| `flow-checkout-desktop.html` | Check-out · 🖥 | The same flow as the closing **"Your stay"** sidebar destination: settled checklist + add-ons left, a **sticky action ledger** right, and a **stage-aware Vela rail** (check-out timeline · deposit-hold explainer · keys · airport transfer). Two-column thank-you with side-by-side review platforms, the processing → receipt confirmation stage, the low-rating repair screen and the completed-state summary |
| `flow-esim-v2.html` | Travel eSIM · 📱 | A rebuild of `pages/esim` (`ManageEsimView` + `ChoosePlanView` + `MyEsimsView`/`MyEsimPlans`/`EsimHelp`). The hub leads with the **destination and a price**, not a definition of "eSIM", and is **state-aware** — an un-installed eSIM promotes an "install it now" card, the handoff the shipped flow never makes after `submitHandler` navigates into Payments. Plans become **radio cards behind a duration rail**, the group covering the guest's real stay preselected, a **€/GB unit price** on every card, and **"Best value" computed as the cheapest per GB** (shipped: stamped on `package_is_unlimited`); the quantity stepper moves **inside the card it controls**, and multi-package buying survives as an explicit "Add a different plan". The **install ladder** runs in the order that succeeds on the phone in the guest's hand — ① one-tap install (iOS) / manual codes (Android), ② manual codes, ③ **QR relabelled "Scan from another device"** and drawn large enough to scan (shipped: 80 px, first, to be scanned by the phone showing it), ④ APN demoted to a collapsed troubleshooting rung. Copy buttons **confirm**; the FAQ is **closed** by default; data usage is an **honest pointer**, never an invented meter. Ten states incl. the reserving wait, both empty states and a skeleton |
| `flow-esim-desktop-v2.html` | Travel eSIM · 🖥 | The same views as the navy-sidebar **"Travel eSIM"** destination: the mobile dock becomes a **sticky itemised ledger** (plan · devices · total, plus the three things the guest is about to wonder), and the install screen admits that **a browser cannot install an eSIM** — it leads with a scannable QR aimed at the guest's phone, the one context where the QR really is the right first rung |
| `flow-chat-mobile.html` | Chat with host · 📱 | Interactive guest ↔ host chat (`pages/ChatView`: `ChatInterface` + `MessageBubble` + `ChatInput` + `OtpVerification`): the "Talking with {property}" thread becomes a real conversation with a **host identity header** (avatar + presence dot + verified tick + response-time line) and a **trip strip** (property · dates · guests); **grouped bubbles** with **day separators** and a proposed **Sending → Sent → Delivered → Read** receipt ladder (keeping the failed + retry state); a warm **first-message** state with tappable **starter questions** instead of `no_messages_yet`; the `useChatOtp` **identity check** reframed as a full screen with rationale (was a dialog over a blurred thread); image/file **attachments** with a lightbox and pending-upload chips; and honest **reconnecting**, **chat-off** (→ FAQ / guidebook) and **register-first** (→ registration) states. Modals: booking details sheet, image lightbox |
| `flow-chat-desktop.html` | Chat with host · 🖥 | The same flow as the **"Support" → "Chat with host"** navy-sidebar destination: the thread in the middle and a right **context rail** carrying the host card (photo · verified · response time), the shared **booking** summary, **common questions** (fill the composer) and a **shared-files** gallery — the one "Talking with {property}" line becomes a persistent shared-context panel. Covers the conversation, first-message, identity-check and chat-off states |
| `flow-error-mobile.html` | Error & recovery · 📱 | Interactive full-page error states (`pages/ErrorView` + `ErrorFallback` + `SDKErrorView`), each with a real way out: **invalid link** (broken-link mark → "Find my booking" into the search flow + contact host + what-to-check list), **too many requests** (clock mark + countdown dial inviting a retry), **something went wrong** (crash mark + copyable **error reference** + reload/support), and the **SDK embedded** variant shown inside a mock host website |
| `flow-error-desktop.html` | Error & recovery · 🖥 | The same four error states as a centered **glass card** on the branded gradient (errors are pre-auth — no app shell), reusing the search flow's pre-auth shell; SDK embedded shown inside a wider mock host page |
| `vela.html` | Building blocks | Vela helper: desktop glass rail, mobile FAB + sheet, and every building block isolated |
| `modals-demo.html` | Building blocks | QR handoff, share link, photo privacy, skip confirm and the Vela sheet — dialogs vs bottom/right sheets |
| `scroll-demo.html` | Building blocks | Sticky glass chrome / CTA dock / pinned nav scroll behavior on long mobile and desktop layouts |
| `shared.css` | — | Design tokens (copied from `@chekinapp/tokens` values) + shared primitives + liquid-glass + Vela + desktop shell |
| `iv.css` / `ivd.css` / `reg.css` / `deals.css` / `home.css` | — | Per-flow styles for IV mobile / IV desktop / registration / offers modal / home |
| `pay.css` / `payd.css` | — | Payments styles: mobile + shared primitives (`pay.css`), desktop additions (`payd.css` — desktop page loads both) |
| `tax.css` | — | Taxes styles (math card, exemption form, delta preview, paid banner) — layered on `pay.css` primitives; the desktop page also reuses `payd.css` |
| `deposit.css` | — | Property-protection styles: the checkbox variant of `.opt-card`, the **"leaves your account today"** row, the paid/locked card, the payment-type radio (pay online / pay on arrival), the consequence strip above the CTA, the info-modal body (how-it-works timeline, covered/not-covered lists), the waiver reader and the two-column desktop chooser — layered on `pay.css`, whose `.opt-card` it extends rather than forks |
| `book.css` | — | Book-an-experience styles: the offer strip, named multi-price items, time-based radio slots with the free standard hour, the delivery day-strip + hour select, the running total (dashed when the host must approve), the confirm-email panel, and the deep-link resolver + toast — layered on `pay.css` + `upsell.css` (the quantity stepper `.qty-row`/`.stepper` comes from upselling and is not redefined) |
| `upsell.css` | — | Upselling styles (category rail, lane chips, offer/partner/request cards, approval tracker, booking box) **plus the partner banners & "Handpicked for your trip" recommendations** (Mozio "HOT DEAL" transfer banner, Airalo eSIM banner, GuruWalk free-offer cards, reco grid, Free-offers FAB) — the banner/reco classes depend only on `shared.css`, so the Home pages load `upsell.css` without `pay.css`; the flow pages layer it on `pay.css` (desktop also `payd.css`) |
| `faq.css` | — | FAQ & language styles (search pill, `<details>` accordion, highlight mark, empty state, language panel + option rows) — layered on `pay.css` primitives; both form factors load it |
| `auth.css` | — | Sign-in & onboarding styles (pre-auth branded gradient stage + orbs, the liquid-glass auth card, value-prop perks, password strength, OTP cells, Google/social button, the labelled onboarding step rail, reservation-type & lead-guest **choice cards**, guest **counter** steppers, who's-coming **avatar rows** + agreement clause, the guest-creation success modal, and the desktop **brand-panel split**) — layered on `shared.css` only; both form factors load it. Also holds the **`html.glass-lite`** Minimal-glass overrides for the shared `glass-toggle.js` switch |
| `search-v2.css` | — | Find-booking **V2** styles (the CSS-painted property photo + SVG-noise grain + legibility scrim, the frosted sheet with its specular sweep, host strip, method cards, segmented code cells, the scan card + step checklist, the boarding pass with masked tear notches, actionable not-found rows, grouped create-reservation, and the desktop split canvas) — layered on `shared.css` only; both form factors load it. Carries its own **`html.glass-lite`** Minimal overrides (the hero photo is exempt — it is content, not chrome) |
| `search-v2.js` | — | Find-booking **V2** interactions, prototype-only: auto-typing / Paste / clear for the segmented code cells, the searching checklist, the copy-reference affordance. Detects screen activation with a `MutationObserver`, so it composes with `flow.js` without either knowing about the other |
| `search.css` | — | Find-booking **V1** / search-reservation styles (property-context chip, method cards, big uppercase code input, "where's my code?" affordance, searching radar + success/warn states, actionable not-found rows, "From your search" preload tags, grouped create-reservation form, and the desktop pre-auth card shell) — layered on `shared.css` primitives; both form factors load it |
| `err.css` | — | Error & recovery styles (variant illustration rings for invalid-link / rate-limit / crash, "what to check" checklist, rate-limit countdown dial, copyable error-reference chip, and the mock host-site frame + embedded widget card for the SDK variant) — reuses the search pre-auth glass shell (`search.css`); both form factors load `shared.css` + `search.css` + `err.css` |
| `guide.css` | — | Guidebooks styles (browsable cover cards + type badge + preview chips, the EDITOR reading view: hero, sticky chapter rail, Wi-Fi copy card, info rows, amenities grid, CSS-drawn map block, place/recommendation cards, file & video blocks, the PDF doc-reader toolbar + HTML reader frame, the unavailable/empty states, the jump-to-guidebook drawer, and the desktop grid + chapter-nav rail) — layered on `shared.css` only; both form factors load it. Also holds the **`html.glass-max`** overrides for the "Liquid glass" version (see `guide-toggle.js`) |
| `guide-toggle.js` | — | The **Version: Classic · Liquid glass** A/B switch injected on both Guidebooks pages; flips `html.glass-max` (overrides in `guide.css`) to extend frosting onto the reading surfaces over color orbs, persisted in localStorage |
| `hguide.css` | — | Housing-guidebook (property-link) chrome only: the 64px mobile property header, the brand block (logo tile + housing name, no booking ref), the "no booking needed" strip, the desktop `284px + auto` shell and its 94px `contentTitle` header, the nav drawer rows + footer, the language-fallback note, the empty state and the card/nav skeletons. The cards and reading blocks come from `guide.css`, which both housing pages also load — the surfaces are the same components, only the shell differs |
| `keycards.css` | — | Kiosk key-card styles: the kiosk frame + the **physical dispenser slot** drawn into its bezel (idle / `.live` / jammed), kiosk-sized buttons (84px) and status marks, the **card stack** (`.kc.done` solid-with-chip · `.kc.pending` dashed ghost · `.kc.printing`), the `ProgressTaskBar` port, the dispenser call-out with its bobbing arrow, the bottom-sheet overlays for `RegisteredGuestsInfoModal` / `PrintedCardsModal`, and the registered/pending tally — layered on `shared.css` only |
| `chat.css` | — | Chat-with-host styles (host-identity header + presence dot + trip strip, the thread: day separators, grouped host/guest bubbles, delivery/read receipts, typing indicator, image/file bubbles; the composer with pending-upload chips + soft char counter; the starter-question chips; the reframed OTP verify screen; the chat-off / register-first state screens; and the desktop **context rail** — host card, booking rows, common questions, shared-files gallery) — layered on `shared.css` only; both form factors load it |
| `chat-demo.js` | — | Prototype-only chat interactions: auto-grow the composer textarea + soft char counter, toggle the send button with content (mirrors `ChatInput.canSend`), fill the composer from starter chips / common-question rows (`data-fill`), and open the image lightbox — no business logic |
| `chat-toggle.js` | — | The **Version: Standard · Liquid glass** A/B switch injected on both Chat pages; flips `html.glass-max` (overrides in `chat.css`) to frost the reading surfaces (host bubbles, trip strip, day chips, starter chips, desktop context rail) over color orbs, persisted in localStorage (`protoChatGlass`) |
| `keys.css` | — | Virtual keys / remote-access styles (door cards with building/apartment icons, keypad code + copy, the **slide-to-open** control, the conditions-gate checklist, before-the-window locked rows, Keyless steps + booking-ref, Salto multi-room group, opening/opened/error status animations) — layered on `pay.css` primitives; both form factors load it |
| `esim-v2.css` / `esim-v2.js` | — | Travel eSIM V2 styles (destination hero, compatibility strip + requirements checklist, duration rail, radio plan cards with €/GB and an in-card quantity stepper, the mobile dock ↔ desktop ledger, the purchased-eSIM card with its three-state rail, the install ladder + numbered step rail, copyable code rows, the CSS-drawn QR, the honest-usage pointer, help card + FAQ, empty states and skeletons) — layered on `shared.css` only; both form factors load it. `esim-v2.js` holds the prototype-only interactions: plan selection → running total, the in-card quantity stepper, cloning a group for "Add a different plan", the iOS↔Android segmented control, **copy-with-confirmation** (label flip + toast + `aria-live`), "I’ve installed it" flipping the card status, and a deterministic seeded (`Math.random`-free) mock QR painter |
| `travel-guide-v2.css` / `travel-guide-v2.js` | — | AI Travel Guide V2 styles (the CSS-painted destination hero with its horizon silhouette, SVG-noise grain and legibility scrim; the "Tuned for" assumption chips; question cards as real `fieldset`/`legend` groups with radio/checkbox chips, the split required/optional meter and per-question errors; the build ladder with its elapsed clock; the three-exit failure card; the "Right now" card; sticky glass phase tabs and day chips; time-of-day headings; slot cards with CSS-painted scene thumbnails, start-time chips, the shape-and-word cost band, the "Why this pick" disclosure with its provenance row, and partner labelling; the check-in card; the offline banner; shape-matched skeletons; the desktop trip rail; and `print` styles that drop the chrome and open every "Why this pick") — layered on `shared.css` only; both form factors load it. `travel-guide-v2.js` holds the prototype-only interactions: required-vs-optional counting, soft caps that disable rather than silently evict, an always-enabled submit that flags and focuses the first gap, real tablist keyboard handling (←/→/Home/End, roving `tabindex`), the deterministic build ladder (`Math.random`/`Date.now` free), add-to-stay and copy confirmations with `aria-live`, and Escape-to-close. Same `MutationObserver` composition trick as `search-v2.js` |
| `splash-toggle.js` | — | The **Style: Classic · Modern** switch injected on both Splash pages; flips `html.sp-classic` (overrides in `splash-v2.css` §9.5), persisted in localStorage (`protoSplashStyle`). Unlike the other toggles this one is not a glass A/B — the two treatments are **two shippable designs of the same component**, identical in behaviour, states and accessibility; only the surface differs |
| `splash-v2.css` / `splash-v2.js` | — | Splash screen styles (the splash surface with its brand aurora and derived-ink contrast guard, the reserved 96 px logo plate + honest progress ring, the quiet stage line, the slow/error/offline recovery blocks, the copyable error reference, the "Secure check-in" trust footer, the shape-matched skeletons the splash dissolves into, the desktop / SDK-widget / kiosk shells, the **Classic** overrides (§9.5 — flat ground, bare logo, dots, no glass), and a **faithful replica of today's splash** — the 3 s fade-in, the dot pulse, and the curtains reproduced with the `ease` fallback the invalid two-argument `cubic-bezier(0, 1.23)` actually produces) — layered on `shared.css` only; both pages load it. `splash-v2.js` holds the prototype-only interactions: the four timing constants and the per-scenario ladders, the ring that eases to 90 % and stops, the handoff (fade + fly-to-header + focus the arrived `<h1>`), the 20 s watchdog, copy-with-confirmation, and the V1 replica's clock + dead-screen counter. Deterministic — no `Date.now`, no `Math.random`, so the two phones can be compared frame for frame. Same `MutationObserver` composition trick as `search-v2.js` |
| `guests-v2.css` / `guests-v2.js` | — | Guests summary V2 styles (progress line + segmented per-guest meter, the four row states with their rail shapes, kebab-as-sibling, named empty slots, docked CTA / desktop action ledger, celebration seal, shape-matched skeletons, the refetch sweep, the Undo toast, delete-confirm and the group steppers) — layered on `shared.css` only; both form factors load it. The mobile page also loads `reg.css` for the V1 comparison phone. `guests-v2.js` holds the prototype-only interactions: delete → optimistic row removal → **Undo** (10 s), copy-link confirmation, counter steppers, Escape-to-close |
| `sign.css` / `sign.js` | — | Guest signature (maps `SignatureCanvas` + `SignatureModal`): the interactive states (empty / drawing / signed / clear / required-error) and the form-factor split — **desktop draws inline**, **mobile taps to open the sign sheet** then previews. Loaded by both registration files |
| `autofill.css` | — | Autofill MRZ-scan modal (maps `AutoFillFlowModal` / `CameraCapture` / backend OCR): the glass modal header, the dark camera frame with the highlighted MRZ band + scan line + glass status pill, success/error overlays, capture hints + Need-help sheet, the document-selection selects, and the on-form scan result (summary + ✦ Scanned tags + Check flags). Loaded by both autofill flow files |
| `flow.js` / `modals.js` / `nav.js` | — | Scenario/screen switcher for flows, modal triggers, and the floating feature-first prototype nav. The nav opens with a **search field** (press <kbd>/</kbd> anywhere) that filters flows by name, group, badge (`proposal`, `current`) or author (`opus`, `fable`); <kbd>↑</kbd>/<kbd>↓</kbd> move, <kbd>Enter</kbd> opens, <kbd>Esc</kbd> clears then closes. Search matches **flows only** — a flow's scenarios stay in its hover flyout |
| `housing-iv-v2.css` | — | Verify-on-housing **V2** styles — only what this flow adds on top of `search-v2.css`: the **journey preview**, the **presence banner**, real form controls (`.hv-input`, 52 px, 16 px type), the inlined **"one more detail" notice** + already-provided chips, the **match card**, the editable "you searched for" summary, the **entrance QR sticker** (`.hv-sticker`) and the "why we're asking" wrapper (`.hv-cam-why`) that sit inside the IV flow's own viewfinder, the two **alert boxes**, a **defect callout** (`.hv-bug`), and an `.sr-only` live-region primitive. The scanner and the verification-unavailable screen are **not styled here at all** — both pages load `iv.css` / `ivd.css` and render those IV screens verbatim
| `housing-iv-v2.js` | — | Verify-on-housing **V2** interactions, prototype-only: pushes each screen's `data-hv-announce` into the frame's `aria-live` region, moves focus to `[data-hv-focus]` on screen activation (never on first paint), and repaints the desktop frame's URL bar from `data-hv-url` so it tracks the crossing from the SDK page into the guest's own session. Same `MutationObserver` composition trick as `search-v2.js`
| `glass-toggle.js` | — | The **Full · Minimal** liquid-glass A/B switch injected on the Find-booking, Error & Sign-in/Onboarding pages; flips `html.glass-lite` (overrides in `search-v2.css`, `search.css` + `auth.css`), persisted in localStorage |

## Ground rules used

- **No new colors** — every value comes from `packages/tokens` in `../chekin-ui` (brand `#385bf8`, navy `#161643`, cool lavender neutrals, Poppins).
- **Liquid glass is chrome-only** — `backdrop-filter` on top bars, bottom nav, on-camera pills and CTA docks; never on dense reading surfaces. Solid `rgba` fallbacks included. (The pre-auth **Find booking** and **Error & recovery** flows lean into it further: the floating auth card, method cards, property chip and language pill are frosted over color orbs — but the input fields stay solid so entered text keeps contrast.)
- **Two glass versions, toggleable** — the Find-booking, Error and **Sign-in/Onboarding** pages carry a **"Glass style: Full · Minimal"** switch (top of the page, via `glass-toggle.js`). **Full** is the frosted/orb version (the default, and the current design); **Minimal** (`html.glass-lite`) reverts those extra surfaces to solids and flattens the stage orbs, keeping only the baseline chrome glass and the brand gradient. The choice persists across pages (localStorage) so the variants can be compared side by side. Overrides live in `search.css` (Find-booking / Error) and `auth.css` (Sign-in / Onboarding). The **Guidebooks** pages carry the mirror-image switch **"Version: Classic · Liquid glass"** (via `guide-toggle.js`): here **Classic** (default) is the chrome-only shipped look and **Liquid glass** (`html.glass-max`) is the extra-frosted one — it adds glass to the reading cards/search/map/place cards over color orbs, deliberately relaxing the chrome-only rule so the two treatments can be compared. The **Chat** pages carry the same switch **"Version: Standard · Liquid glass"** (via `chat-toggle.js`, `html.glass-max` overrides in `chat.css`): **Standard** (default) keeps glass on chrome only with solid bubbles; **Liquid glass** frosts the host bubbles, trip strip, day chips, starter chips and desktop context rail over color orbs.
- **The Splash switch is not a glass A/B.** The other toggles compare two treatments of one agreed design. **Style: Classic · Modern** on the Splash pages compares *two shippable designs of the same component*: **Classic** is the shipped V1 with its four defects fixed and nothing else touched (flat brand background, bare logo, three pulsing dots, curtains removed); **Modern** rebuilds the surface in the pre-auth idiom (branded gradient, colour orbs, film grain, liquid-glass plate, honest progress ring). The timing contract, the states, the handoff and the accessibility pass are **identical in both** — the choice is purely aesthetic, and either can ship.
- **Motion respects `prefers-reduced-motion`.**
- Poppins is loaded from Google Fonts for the prototype only; the app self-hosts the same family.
